apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: helm-k8s-app2
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - git:
      repoURL: https://github.com/mteodor/argocd-example-apps.git
      revision: appset-demo-mirko-sanity-demo
      files:
      - path: "appset-demo/**/values.yaml"
      values:
        appName: '{{index .path.segments 1}}'
        envName: '{{index .path.segments 2}}'
        clusterName: '{{index .path.segments 3}}'
  template:
    metadata:
      name: 'helm-{{.values.appName}}-{{.values.envName}}-{{.path.basename}}'
      labels:
        harness.io/envRef: '{{.values.envName}}'
        harness.io/serviceRef: '{{.values.appName}}'
        cluster: '{{.values.clusterName}}'
    spec:
      project: 937fb9b4
      source:
        repoURL: https://github.com/mteodor/argocd-example-apps.git
        targetRevision: appset-demo-mirko-sanity-demo
        path: "helm-guestbook"
        helm:
          parameters:
            - name: version
              value: '{{.releaseTag}}'
            - image:
                tag: '{{.releaseTag}}'
      destination:
        name: '{{.values.clusterName}}'
        namespace: mirko-sanity-demo
