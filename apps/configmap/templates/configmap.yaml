apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/name: argocd-cm
    app.kubernetes.io/part-of: argocd
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
data:
  {{ .Values.sampleKey }}: {{ .Values.sampleValue }}
